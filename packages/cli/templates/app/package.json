{
  "name": "{{projectName}}",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx vibe dev",
    "build": "npx vibe build",
    "start": "node dist/main.js",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "validate": "tsc --noEmit && eslint src/ && vitest run && npx vibe build",
    "db:migrate": "drizzle-kit push",
    "db:seed": "tsx src/database/seeds/index.ts",
    "db:studio": "drizzle-kit studio"
  },
  "dependencies": {
    "@vibeonrails/core": "workspace:*",
    "@vibeonrails/infra": "workspace:*",
    "@vibeonrails/cli": "workspace:*",
    "hono": "^4.0.0",
    "drizzle-orm": "^0.29.0",
    "postgres": "^3.4.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "drizzle-kit": "^0.20.0",
    "eslint": "^9.0.0",
    "tsup": "^8.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
